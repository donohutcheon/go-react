# https://devcenter.heroku.com/articles/heroku-yml-build-manifest
# Officially unsupported, but works.
build:
  config:
    BUILD_ENV: staging
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - ./static/build/deployment-tasks.sh